import 'package:flutter/material.dart';

class AppTheme {
  static final ThemeData lightTheme = ThemeData.light().copyWith(
      textTheme:
          const TextTheme(bodyText1: TextStyle(fontFamily: 'NotoSerif')));
}
